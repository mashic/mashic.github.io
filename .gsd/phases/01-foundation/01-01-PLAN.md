---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [angular.json, package.json, src/app/*, src/styles.scss, src/index.html]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ng serve runs and displays the portfolio shell"
    - "Navbar is fixed at top with section links"
    - "Hero section shows developer name, role, and CTA"
    - "Clicking navbar links scrolls to correct sections"
  artifacts:
    - "src/app/components/navbar/navbar.component.ts"
    - "src/app/components/hero/hero.component.ts"
    - "src/app/app.component.ts"
    - "src/styles.scss"
  key_links:
    - "AppComponent renders NavbarComponent and HeroComponent"
    - "Navbar links target section IDs in AppComponent template"
---

<objective>
Create Angular project scaffold, app shell with section layout, fixed navbar with smooth scroll, and hero section.

Purpose: Establish the foundation — running Angular app with navigation and first visual section.
Output: Working Angular app with navbar + hero, buildable and serveable.
</objective>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/research/STACK.md
@.gsd/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Angular project</name>
  <files>angular.json, package.json, tsconfig.json, src/</files>
  <action>
  Run `ng new portfolio --style=scss --routing=false --ssr=false --skip-git` in a temp location, then move the contents into the repo root (preserving existing .git, .gsd, rss-suite, plan.md). The old index.html and style.css will be replaced.
  Configure angular.json: set outputPath to "docs" for GitHub Pages deployment.
  Update index.html title to "Masic | Full-Stack Developer".
  </action>
  <verify>npm install succeeds, ng serve compiles without errors</verify>
  <done>Angular app created, dependencies installed, build targets docs/ folder</done>
</task>

<task type="auto">
  <name>Task 2: Global styles and CSS variables</name>
  <files>src/styles.scss</files>
  <action>
  Define SCSS variables for the design system:
  - Color palette: dark background (#0a0a0a), light text (#e0e0e0), accent (#64ffda), secondary (#8892b0), card bg (#112240)
  - Typography: system font stack (Inter, -apple-system, sans-serif)
  - Spacing scale and section padding
  - Global reset (box-sizing, margin, scroll-behavior smooth)
  - Responsive breakpoints as mixins
  </action>
  <verify>Styles compile without errors</verify>
  <done>Global SCSS with variables, reset, and mixins defined</done>
</task>

<task type="auto">
  <name>Task 3: App shell layout</name>
  <files>src/app/app.component.ts, src/app/app.component.html, src/app/app.component.scss</files>
  <action>
  Create the root app component as the single-page shell:
  - Template includes navbar and all section placeholders (hero, about, skills, projects, experience, architecture, contact)
  - Each section wrapped in a container with an id attribute for scroll targeting
  - Use standalone component pattern (Angular 19 default)
  - Import all child components
  </action>
  <verify>App compiles with section structure visible</verify>
  <done>App component renders all sections in order with IDs for navigation</done>
</task>

<task type="auto">
  <name>Task 4: Navbar component</name>
  <files>src/app/components/navbar/navbar.component.ts, navbar.component.html, navbar.component.scss</files>
  <action>
  Create NavbarComponent (standalone):
  - Fixed position at top, semi-transparent dark background with backdrop blur
  - Logo/name on left
  - Navigation links: About, Skills, Projects, Experience, Architecture, Contact
  - Click handler that uses document.getElementById + scrollIntoView({ behavior: 'smooth' })
  - Mobile: hamburger menu toggle (CSS-only or simple boolean toggle)
  - Responsive: links collapse to hamburger on mobile
  </action>
  <verify>Navbar renders, links scroll smoothly when sections have matching IDs</verify>
  <done>Fixed navbar with working smooth scroll navigation, responsive hamburger on mobile</done>
</task>

<task type="auto">
  <name>Task 5: Hero component</name>
  <files>src/app/components/hero/hero.component.ts, hero.component.html, hero.component.scss</files>
  <action>
  Create HeroComponent (standalone):
  - Full viewport height section
  - Small greeting text ("Hi, my name is")
  - Large name heading ("Masic")
  - Subtitle with role ("Full-Stack Developer")
  - Brief tagline about 7+ business domains
  - CTA button "View My Work" that scrolls to projects section
  - Clean, developer-aesthetic styling with accent color highlights
  </action>
  <verify>Hero section displays at full viewport with all text elements and working CTA</verify>
  <done>Hero with name, role, tagline, and CTA button that scrolls to projects</done>
</task>

</tasks>
